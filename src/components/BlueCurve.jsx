import styled from "styled-components";
import { color } from "../color";

const BlueCurve = styled.div`
  height: 2rem;
  width: 100%;
  border-radius: 50% 50% 0 0;
  background-color: ${color.primary};
`;

export default BlueCurve;
